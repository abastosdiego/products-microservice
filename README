# Necessário docker e docker compose instalados para rodar o ambiente de desenvolvimento.
# Ambiente Node.js com Typescript, nodemon e Jest configurados usando docker-compose.

No terminal, rodar o seguinte comando para subir o ambiente:
```php
docker compose up
```

rodar os testes dentro do container:
```php
docker exec -it product-microservice bash
npx jest
```
